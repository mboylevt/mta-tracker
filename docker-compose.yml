services:
  mta-tracker:
    build: .
    ports:
      - "9876:9876"
    env_file:
      - .env
    environment:
      - TZ=America/New_York
    volumes:
      - ./data/configs:/app/data/configs
    restart: unless-stopped
